// config added for static site generation
const nextConfig = {
  basePath: "/-actualize-converter-app",
  output: "export",

  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
module.exports = nextConfig;
