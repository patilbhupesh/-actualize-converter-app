// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  output: "export",

  basePath: "/-actualize-converter-app",

  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
